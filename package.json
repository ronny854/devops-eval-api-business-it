{
  "name": "devops-eval-api",
  "version": "1.0.0",
  "description": "REST API for DevOps CI/CD technical evaluation.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --coverage --coverageReporters=lcov --coverageReporters=text",
    "lint": "eslint src/ test/"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  },
  "engines": {
    "node": ">=20"
  },
  "jest": {
    "coverageThreshold": {
      "global": {
        "lines": 70
      }
    }
  }
}
